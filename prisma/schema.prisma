generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model User {
  id String @id @default(uuid())

  messageId String? @map("message_id") @db.Uuid

  name  String
  email String @unique
  
  age Int? @db.SmallInt 
}